# 🚀 无需信用卡！完全免费博客部署指南

> **目标**：将 `deepsleep.fun` 域名指向一个完全免费、无需信用卡的博客系统  
> **方案**：Hugo + GitHub Pages / Cloudflare Pages  
> **费用**：$0.00/月（永久免费）  
> **信用卡**：❌ 完全不需要！

---

## ✨ 为什么选择这个方案？

### 对比其他方案

| 方案 | 费用 | 信用卡 | 休眠问题 | 后台管理 |
|-----|------|-------|---------|---------|
| **Hugo + GitHub Pages** ⭐ | **$0** | ❌ 不需要 | ✅ 无 | ⚠️ Markdown写作 |
| Oracle Cloud | $0 | ✅ 需要 | ✅ 无 | ✅ Ghost后台 |
| Railway | $0-20+ | ✅ 需要 | ⚠️ 会休眠 | ✅ Ghost后台 |
| WordPress.com免费版 | $0 | ❌ 不需要 | ✅ 无 | ✅ 有后台 |

### 这个方案的优点：

1. 💰 **真正免费**：
   - GitHub Pages: 100GB 带宽/月，无限存储
   - Cloudflare Pages: 无限带宽和请求
   - **零隐藏费用**

2. 🔒 **安全隐私**：
   - 无需提供任何支付信息
   - 无需实名认证
   - 只需邮箱注册即可

3. ⚡ **性能卓越**：
   - 全球 CDN 加速
   - 访问速度比很多付费主机还快
   - 99.9% 在线时间保证

4. 🎯 **完全控制**：
   - 所有内容在你的 Git 仓库中
   - 版本控制，永不丢失
   - 可以随时迁移到其他平台

5. 📱 **SEO 友好**：
   - 纯静态 HTML，加载速度极快
   - Google 搜索引擎友好
   - 自动生成 sitemap.xml

### 唯一的"缺点"：

⚠️ **没有可视化后台编辑器**
- 需要用 **Markdown** 格式写文章（其实很简单！）
- 或者使用 **VS Code + 实时预览插件**（体验接近后台）
- 也可以使用 **Forestry/Decap CMS**（免费的 Git-based CMS）

---

## 📋 准备工作

### 你只需要准备：

- [x] **域名**: deepsleep.fun（已有 ✅）
- [ ] **GitHub 账号**: 免费注册（只需邮箱）
- [ ] **Git**: 安装版本控制工具
- [ ] **文本编辑器**: VS Code（推荐）或任何编辑器

> 💡 **全程不需要信用卡、不需要手机验证、不需要身份证！**

---

## 🛠️ 第一步：安装本地开发环境

### 1.1 安装 Git（如果还没有）

#### Windows 用户：

1. 访问 https://git-scm.com/download/win
2. 下载并运行安装程序
3. 一路默认选项点击"Next"
4. 安装完成后，打开 PowerShell 或 CMD，输入：
   ```bash
   git --version
   ```
   应该显示：`git version 2.x.x`

#### macOS 用户：

```bash
# 使用 Homebrew（如果没有先安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 Git
brew install git
```

#### Linux 用户：

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install -y git

# CentOS/RHEL
sudo yum install git
```

---

### 1.2 安装 Hugo（静态网站生成器）

#### Windows 用户（推荐使用 Chocolatey 或 Scoop）：

**方式 A：Chocolatey 包管理器**
```powershell
# 先安装 Chocolatey（如果没有）
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# 安装 Hugo
choco install hugo-extended -y
```

**方式 B：Scoop 包管理器**
```powershell
# 安装 Scoop
irm get.scoop.sh | iex

# 安装 Hugo
scoop install hugo-extended
```

**方式 C：手动下载（最简单）**
1. 访问 https://github.com/gohugoio/hugo/releases
2. 下载最新版本的 `hugo_extended_xxx_Windows-64bit.zip`
3. 解压到任意目录（如 `C:\hugo\`）
4. 将路径添加到系统 PATH 环境变量

#### macOS 用户：

```bash
# 使用 Homebrew
brew install hugo
```

#### Linux 用户：

```bash
# Ubuntu/Debian (从 GitHub 下载)
wget https://github.com/gohugoio/hugo/releases/latest/download/hugo_extended_Linux-64bit.deb
sudo dpkg -i hugo_extended_Linux-64bit.deb
```

### 1.3 验证安装

```bash
hugo version
```

预期输出：
```
hugo v0.139.0-xxxxxxx+extended windows/amd64 BuildDate=xxxxxx
```

> ⚠️ **重要**：确保显示 `extended` 字样，否则不支持主题的一些功能！

---

## 🎨 第二步：初始化博客项目

### 方式 A：使用我已创建的项目模板（推荐）

我已经为你创建好了完整的项目结构！

#### 2.1 获取项目文件

项目位置：`c:\Users\26516\Desktop\n8n\blog-static\`

包含以下文件：
```
blog-static/
├── .github/
│   └── workflows/
│       ├── deploy.yml              # GitHub Pages 自动部署
│       └── cloudflare-deploy.yml    # Cloudflare Pages 备选
├── content/
│   ├── posts/
│   │   └── welcome.md             # 示例文章
│   └── about.md                   # 关于页面
├── static/
├── themes/
├── hugo.toml                     # 配置文件
└── README.md                      # 项目说明
```

#### 2.2 初始化 Hugo 模块

```bash
cd c:\Users\26516\Desktop\n8n\blog-static

# 初始化 Git 仓库
git init

# 初始化 Hugo 模块（用于下载主题）
hugo mod init github.com/你的用户名/deepsleep-blog

# 下载 PaperMod 主题
hugo mod get github.com/adityatelange/hugo-PaperMod
```

#### 2.3 本地预览测试

```bash
# 启动本地开发服务器
hugo server -D
```

打开浏览器访问：
- **前台**：http://localhost:1313
- **文章预览**：http://localhost:1313/posts/welcome/

看到博客页面就说明成功了！按 `Ctrl+C` 停止服务器。

---

### 方式 B：从头创建新项目（如果你想自定义）

```bash
# 创建新站点
hugo new site my-blog --format yaml

cd my-blog

# 初始化模块
hugo mod init github.com/你的用户名/my-blog

# 添加主题
hugo mod get github.com/adityatelange/hugo-PaperMod

# 创建第一篇文章
hugo new posts/hello.md

# 启动开发服务器
hugo server -D
```

---

## 🌐 第三步：推送到 GitHub 并自动部署

### 3.1 创建 GitHub 仓库

1. 打开 https://github.com/new
2. Repository name: `deepsleep-blog`（或你喜欢的名字）
3. Description: `DeepSleep Personal Blog`
4. 选择 **Private** 或 **Public**（公开仓库可以免费使用 GitHub Pages）
5. **不要勾选** "Add a README file"
6. 点击 **Create repository**

### 3.2 推送代码到 GitHub

```bash
cd c:\Users\26516\Desktop\n8n\blog-static

# 添加所有文件
git add .

# 首次提交
git commit -m "Initial blog setup with Hugo"

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/deepsleep-blog.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 3.3 启用 GitHub Pages

1. 进入刚创建的 GitHub 仓库
2. 点击 **Settings** 标签页
3. 左侧菜单找到 **Pages**
4. Source 部分：
   - 选择 **Deploy from a branch**
   - Branch: `main` / folder: `/ (root)`
5. 点击 **Save**

等待 1-2 分钟，GitHub 会自动构建并部署你的网站。

### 3.4 获取默认域名

部署成功后，在 Settings → Pages 页面会看到：

```
Your site is ready to be published at:
https://你的用户名.github.io/deepsleep-blog/
```

记下这个地址，稍后配置自定义域名时用到。

---

## 🎯 第四步：绑定自定义域名 deepsleep.fun

### 4.1 配置 GitHub Pages 自定义域名

1. 回到 GitHub 仓库 Settings → Pages
2. 找到 **Custom domain** 输入框
3. 输入：`deepsleep.fun`
4. 点击 **Save**
5. **勾选** "Enforce HTTPS"（强制 HTTPS）

GitHub 会提示你需要配置 DNS 记录。

### 4.2 配置腾讯云 DNS 解析

1. 登录 [腾讯云 DNS 控制台](https://console.cloud.tencent.com/cns)
2. 找到域名 `deepsleep.fun`，点击进入
3. 点击 **添加记录**，添加以下记录：

#### 记录 1：Apex 域名 (@)

| 配置项 | 值 | 说明 |
|-------|-----|------|
| **主机记录** | `@` | 代表根域名 |
| **记录类型** | `CNAME` | 别名记录 |
| **记录值** | `你的用户名.github.io.` | 注意末尾的点！|
| **TTL** | `600` | 10分钟 |

#### 记录 2：www 子域名

| 配置项 | 值 | 说明 |
|-------|-----|------|
| **主机记录** | `www` | www 子域名 |
| **记录类型** | `CNAME` | 同上 |
| **记录值** | `你的用户名.github.io.` | 同上 |

4. 保存设置

### 4.3 验证 DNS 生效

等待 DNS 传播（通常 10-30 分钟），可以使用以下方法检查：

**方法 1：在线检测**
- 访问 https://dnschecker.org/
- 输入 `deepsleep.fun`
- 选择 A/CNAME 类型
- 查看全球各地区的解析结果

**方法 2：命令行检测**
```bash
nslookup deepsleep.fun
```

应该返回类似：
```
Name:    deepsleep.fun
Address: 185.199.xxx.xxx  # GitHub 的 IP
```

### 4.4 测试访问

DNS 生效后，访问：
- ✅ https://deepsleep.fun （主站）
- ✅ https://www.deepsleep.fun （www 重定向）
- ✅ https://你的用户名.github.io/deepsleep-blog/ （备用地址）

所有地址都应该能正常访问且显示 🔒 锁图标（HTTPS）。

---

## ✍️ 第五步：开始写作和发布

### 5.1 创建新文章

#### 方法一：使用命令行

```bash
cd c:\Users\26516\Desktop\n8n\blog-static

# 创建新文章
hugo new posts/我的第二篇文章.md
```

这会在 `content/posts/我的第二篇文章.md` 创建文件，编辑它：

```markdown
---
title: "我的第二篇文章"
date: 2026-05-30T12:00:00+08:00
draft: false
summary: "这是文章摘要，会显示在列表页"
tags: ["技术", "教程"]
categories: ["学习笔记"]
---

# 文章标题

文章正文内容...

## 二级标题

更多内容...

- 列表项 1
- 列表项 2

```python
# 代码示例
print("Hello, DeepSleep!")
```

> 引用文字...

![图片描述](图片链接)
```

#### 方法二：手动创建文件

直接在 `content/posts/` 目录下新建 `.md` 文件即可。

### 5.2 本地预览

```bash
# 启动开发服务器（支持热重载）
hugo server -D

# 浏览器访问 http://localhost:1313
# 修改文件后自动刷新
```

### 5.3 发布到线上

```bash
# 1. 构建生产版本
hugo --minify --gc

# 2. 提交更改
git add .
git commit -m "Add new post: 我的第二篇文章"
git push origin main

# 3. GitHub Actions 会自动构建并部署（约 1-2 分钟）
```

访问 https://deepsleep.fun 即可看到新文章！

---

## 🎨 第六步：个性化定制

### 6.1 修改站点信息

编辑 `hugo.toml`:

```toml
baseURL: 'https://deepsleep.fun/'
languageCode: 'zh-CN'
title: 'DeepSleep Blog'  # ← 改成你喜欢的名字
theme: ['PaperMod']

params:
  homeInfoParams:
    Title: "🌙 你的博客标题"
    Content: >
      这里是副标题或简介...
      
      可以写多行内容。
```

### 6.2 更换主题

PaperMod 主题已经很好看，但如果你想要其他风格：

1. 访问 https://themes.gohugo.io/
2. 选择喜欢的主题
3. 替换 `hugo.toml` 中的 theme 设置
4. 重新下载主题模块

**热门主题推荐**：
- **PaperMod**（当前使用）：简洁现代，适合技术博客
- **Blowfish**：功能丰富，支持深色模式
- **Stack**：极简主义，速度快
- **DoIt**：功能全面，中文友好

### 6.3 添加社交链接

在 `hugo.toml` 中修改：

```toml
params:
  socialIcons:
    - name: github
      url: "https://github.com/你的用户名"
    - name: twitter
      url: "https://twitter.com/你的账号"
    - name: email
      url: "mailto:your@email.com"
    - name: rss
      url: "/index.xml"
```

### 6.4 配置导航菜单

```toml
menu:
  main:
    - identifier: posts
      name: 文章
      url: /posts/
      weight: 10
    - identifier: tags
      name: 标签
      url: /tags/
      weight: 20
    - identifier: categories
      name: 分类
      url: /categories/
      weight: 30
    - name: 关于
      url: /about/
      weight: 40
```

### 6.5 添加评论系统（可选）

#### Giscus 评论（基于 GitHub Discussions）

1. 访问 https://giscus.app/
2. 填写你的仓库信息
3. 生成配置代码
4. 在 `layouts/partials/comments.html` 中粘贴代码

#### Utterances 评论（基于 GitHub Issues）

同样简单易用，详见官方文档。

---

## 📊 第七步：增强功能（可选但推荐）

### 7.1 Google Analytics 统计

1. 注册 [Google Analytics](https://analytics.google.com/)（免费）
2. 获取跟踪 ID（如 `G-XXXXXXXXXX`）
3. 在 `hugo.toml` 中添加：

```toml
params:
  analytics:
    google:
      SiteVerificationTag: "G-XXXXXXXXXX"
```

### 7.2 SEO 优化

Hugo 默认已经做了很多 SEO 优化。额外建议：

1. **生成 sitemap 和 robots.txt**（Hugo 自动生成）
   - 访问 https://deepsleep.fun/sitemap.xml
   - 访问 https://deepsleep.fun/robots.txt

2. **提交到搜索引擎**：
   - [Google Search Console](https://search.google.com/search-console)
   - [Bing Webmaster Tools](https://www.bing.com/webmasters)

3. **Open Graph 和 Twitter Cards**（主题已内置）

### 7.3 图片优化

- 使用 WebP 格式替代 JPEG/PNG
- 压缩图片大小（TinyPNG, Squoosh）
- 使用图床服务（如 GitHub, Imgur, SM.MS）

---

## 🔧 第八步：日常维护

### 8.1 定期更新 Hugo 版本

```bash
# 检查当前版本
hugo version

# 更新 Hugo（根据你的安装方式）
choco upgrade hugo-extended -y  # Windows Chocolatey
brew upgrade hugo                # macOS Homebrew
```

### 8.2 更新主题

```bash
# 更新 PaperMod 主题到最新版
hugo mod get -u github.com/adityatelange/hugo-PaperMod

# 查看更新
hugo mod graph
```

### 8.3 备份内容

由于所有内容都在 Git 仓库中，**备份就是推送代码**：

```bash
# 定期提交
git add .
git commit -m "Regular backup $(date +%Y-%m-%d)"
git push origin main
```

GitHub 会保存所有历史版本，永不丢失！

---

## 🆚 备选方案：Cloudflare Pages（速度更快）

如果你发现 GitHub Pages 在某些地区访问慢，可以改用 Cloudflare Pages：

### 优势：
- ⚡ **全球 CDN 速度更快**（特别是在中国）
- 📈 **无限带宽和请求数**
- 🆓 **同样完全免费**
- 🔒 **同样无需信用卡**

### 迁移步骤：

1. **登录 Cloudflare Dashboard**
   - 访问 https://dash.cloudflare.com
   - 注册账号（只需邮箱，无需信用卡）

2. **添加域名**
   - 将 `deepsleep.fun` 的 NS 服务器改为 Cloudflare 提供的
   - 或者在腾讯云只修改部分记录

3. **创建 Pages 项目**
   - 左侧菜单选择 **Workers & Pages**
   - 点击 **Create project**
   - 选择 **Connect to Git**
   - 授权 GitHub 仓库
   - 构建设置：
     - Framework preset: **None**
     - Build command: `hugo --minify --gc`
     - Build output directory: `public`

4. **配置自定义域名**
   - 在 Pages 项目设置中添加 `deepsleep.fun`
   - Cloudflare 会自动处理 SSL 证书

详细步骤可参考 `.github/workflows/cloudflare-deploy.yml` 配置文件。

---

## 🎉 完成！

恭喜你！你现在拥有了一个：

✅ **完全免费**的博客（$0/月）  
✅ **无需信用卡**（只需邮箱注册）  
✅ **自定义域名** https://deepsleep.fun  
✅ **HTTPS 加密**（自动证书）  
✅ **全球 CDN 加速**（访问速度快）  
✅ **7×24 小时在线**（无休眠问题）  
✅ **版本控制**（内容永不丢失）  

### 快速参考卡

| 操作 | 命令 |
|------|------|
| **本地预览** | `hugo server -D` |
| **创建文章** | `hugo new posts/title.md` |
| **构建生产** | `hugo --minify --gc` |
| **发布上线** | `git push origin main` |
| **查看日志** | GitHub 仓库 → Actions |

---

## 📚 学习资源

### Hugo 官方文档
- https://gohugo.io/getting-started/quick-start/
- https://gohugo.io/content-management/

### PaperMod 主题文档
- https://adityatelange.github.io/hugo-PaperMod/

### Markdown 语法速查
- https://www.markdownguide.org/basic-syntax/

### Git/GitHub 教程
- https://docs.github.com/en/get-started

---

## ❓ 常见问题

### Q1: 必须会用 Git 吗？
**A**: 是的，但很简单。基本操作只有 4 个命令：
```bash
git add .        # 保存更改
git commit -m "" # 写注释
git push         # 上传到 GitHub
git pull          # 下载最新版本
```

### Q2: Markdown 难学吗？
**A**: 非常简单！5 分钟就能掌握基础语法。而且有 VS Code 实时预览插件，所见即所得。

### Q3: 能否迁移回 Ghost/WordPress？
**A**: 当然可以！所有文章都是标准 Markdown 格式，可以轻松导入其他平台。

### Q4: 流量有限制吗？
**A**: 
- GitHub Pages: 100GB 带宽/月（对个人博客绰绰有余）
- Cloudflare Pages: 无限带宽
- 如果不够，随时可以迁移到付费主机

### Q5: 如何添加动态功能（评论、搜索等）？
**A**: 
- **评论**: Giscus, Utterances（基于 GitHub，免费）
- **搜索**: Fuse.js（前端实现，无需后端）
- **表单**: Formspree, Netlify Forms（免费额度足够）

---

## 🚀 下一步行动清单

- [x] 安装 Git 和 Hugo
- [x] 初始化博客项目
- [x] 推送到 GitHub
- [x] 启用 GitHub Pages
- [ ] 配置自定义域名 deepsleep.fun
- [ ] 发布第一篇正式文章
- [ ] （可选）配置评论系统
- [ ] （可选）添加 Google Analytics
- [ ] 开始创作！🎉

---

## 💬 需要帮助？

如果在部署过程中遇到任何问题：

1. **查看本文档**的对应章节
2. **检查 GitHub Actions 日志**：仓库 → Actions → 最新一次运行
3. **搜索错误信息**：Google/Stack Overflow
4. **查阅 Hugo 官方文档**：https://gohugo.io/
5. **随时问我**：我可以帮你诊断具体问题

---

**祝你博客运营顺利！享受创作的乐趣吧！** ✨🎉